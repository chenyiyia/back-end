<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish(name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
        values(#{name},#{categoryId},#{price},#{image},#{description},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>

    <update id="update">
        update dish
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id=#{categoryId},
            </if>
            <if test="price != null">
                price=#{price},
            </if>
            <if test="image != null">
                image=#{image},
            </if>
            <if test="description != null">
                description=#{description},
            </if>
            <if test="status != null">
                status=#{status},
            </if>
        </set>
        <where>
            id=#{id}
        </where>
    </update>

    <delete id="deleteBatch">
        delete from dish where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select t1.*,t2.name from dish t1 left outer join category t2 on t1.category_id=t2.id
        <where>
            <if test="name != null">
                and t1.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and t1.status = #{status}
            </if>
            <if test="categoryId != null">
                and t1.category_id = #{categoryId}
            </if>
        </where>
        order by t1.create_time desc
    </select>
    <select id="getBatchStatusByIds" resultType="java.lang.Integer">
        select status from dish where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    <select id="list" resultType="com.sky.entity.Dish">
        select * from dish where category_id = #{categoryId} and status = #{status};
    </select>
    <select id="getBySetmealId" resultType="com.sky.entity.Dish">
        select t1.* from dish t1 inner join setmeal_dish t2 on t1.id = t2.dish_id where t2.setmeal_id = #{setmeal.id}
    </select>
</mapper>
